# Cute Pattern Studio v0.3 반영 내용 + 다음 단계 계획서

## 1. 이번 v0.3에서 반영한 핵심 수정

### A. 체크 패턴 다양화
기존의 단순 체크/깅엄/손그림 체크 외에 아래 계열을 추가했다.

- 소프트 플래드
- 에어리 플래드
- 파우더 깅엄
- 패브릭 체크
- 소프트 깅엄
- 핸드메이드 플래드
- 텍스처 체크

목표는 사용자가 첨부해준 예시처럼,
**같은 체크라도 결이 다르고 질감이 다른 선택지**를 늘리는 것이었다.

### B. 크기 / 간격 조절 반영 개선
기존에는 일부 체크 계열에서 `크기`나 `간격` 조절이 눈에 잘 띄지 않거나 거의 반영되지 않는 문제가 있었다.

v0.3에서는 특히 체크 계열 렌더 로직을 보정해:
- 패턴 크기
- 패턴 간격
- 불규칙함
이 실제 배열 구조에 더 직접 반영되도록 수정했다.

### C. 업로드 이미지 → 반복 패턴 흐름 개선
사용자가 투명 PNG를 업로드해서 바로 패턴으로 쓰고 싶은데, 흐름이 눈에 띄지 않는 문제가 있었다.

v0.3에서는:
- 왼쪽에 **명확한 업로드 박스** 제공
- 업로드 즉시 **현재 레이어에 자동 연결**
- 업로드 에셋도 `크기 / 간격 / 회전 / 불규칙함` 조절 가능
- `모티프 반복 / 반복 타일` 선택 가능

즉, **투명 PNG를 바로 반복 패턴으로 만드는 흐름**을 더 분명하게 만들었다.

---

## 2. 현재 v0.3 기준 제공 기능 요약

### 패턴 시스템
- 3개 레이어 조합
- 내장 패턴 + 업로드 이미지 패턴 혼합
- 레이어별 개별 색상 / 크기 / 간격 / 회전 / 불규칙함 / 투명도 / 디테일

### 업로드 에셋
- PNG / JPG / WEBP / SVG 업로드
- SVG 단색 변환
- 투명 PNG 반복 패턴화

### 저장/관리
- 즐겨찾기
- 내 프리셋 저장
- 심리스 타일 PNG 저장
- 참고 이미지 팔레트 자동 추출

---

## 3. 다음 단계(v0.4 후보) 기능 목록

### 1순위
1. **업로드 에셋 반복 방식 추가**
   - brick
   - half-drop
   - diagonal repeat
   - random scatter
2. **레이어 순서 변경 / 잠금 / 숨김 강화**
3. **심리스 타일 미리보기 전용 창**
4. **체크 전용 세부 옵션**
   - 가로선 굵기 / 세로선 굵기 분리
   - 이중선 / 삼중선
   - 보조 얇은 선 추가
   - 칸 내부 포인트 밀도

### 2순위
1. **색상 보정 보조 기능**
   - 자동 파스텔화
   - 밝기/채도 간단 조절
   - 최근 사용 색상 기록
2. **JSON 프리셋 내보내기 / 가져오기**
3. **업로드 이미지 외곽선 / 그림자 / 흰 테두리**

### 3순위
1. **패턴 조합 템플릿 추천**
2. **캔바/미리캔버스풍 빠른 배경 세트**
3. **프사 배경용 원형 크롭 미리보기**
4. **애니메이션 움짤용 패턴 시퀀스 출력**

---

## 4. 개발 메모

- 정적 웹앱 구조 유지 (GitHub + Cloudflare Pages 배포 용이)
- 업로드 데이터가 많아지면 localStorage 대신 IndexedDB 검토
- 대형 PNG/SVG 반복 렌더링은 offscreen canvas 최적화 검토
- 업로드 이미지를 더 자유롭게 배치하려면 반복 엔진 확장이 필요
