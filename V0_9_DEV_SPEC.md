# Cute Pattern Studio v0.9 실제 개발 명세서

## 1. 버전 목표

v0.9의 핵심 목표는 다음 4가지다.
1. 체크 패턴을 더 다양하게 확장
2. 오프셋을 버튼으로 미세 조정 가능하게 개선
3. 업로드 에셋 반복 방식에 diagonal repeat 추가
4. 원형 프사 미리보기 제공

## 2. 기능 요구사항

### A. 체크 패턴 신규 추가
추가 대상 예시:
- 3색 체크
- 다이아 체크(마름모 느낌)
- 다이아 깅엄
- 색연필 체크
- 파스텔 크레용 체크

요구사항:
- 기존 4색 팔레트 구조를 그대로 사용
- 사용자가 색상 코드를 자유롭게 바꿀 수 있어야 함

### B. 오프셋 미세 증감 버튼
`위치 X`, `위치 Y` 컨트롤에 추가 버튼 제공:
- -5
- -1
- +1
- +5

추가 요구사항:
- 슬라이더 / 숫자 입력 / 버튼이 서로 동기화
- 오프셋 조정 시 패턴 랜덤 시드가 불필요하게 바뀌지 않도록 처리

### C. Diagonal Repeat
업로드 에셋 반복 방식 `renderMode`에 추가:
- diagonal

동작 규칙:
- 행/열 기준으로 반 칸씩 대각 오프셋을 적용해 사선 리듬의 반복을 만든다.

### D. 원형 프사 미리보기
미리보기 패널 하단에 원형 크롭 프리뷰를 추가한다.
- 현재 메인 캔버스를 원형 마스크로 잘라 표시
- 배경/패턴 조합이 원형 프로필 사진에서 어떻게 보이는지 확인 가능

## 3. 구현 명세

### app.js
- `STORAGE` 키를 v0.9용으로 변경
- `circlePreviewCanvas` 및 `renderCirclePreview()` 추가
- `renderMain()` 후 원형 미리보기 갱신
- 업로드 에셋 `배치 방식`에 `diagonal` 추가
- `renderUploadedLayer()`에 diagonal 반복 로직 추가
- `sliderRow()`에 offset 전용 미세 증감 버튼 추가

### patterns.js
- checker / texturedChecker / plaid 렌더러에 추가 회전 옵션 대응
- tricolor 체크 지원
- 신규 체크 프리셋 및 defaults 추가

### index.html / styles.css
- 원형 프사 미리보기 UI 추가
- v0.9 안내 텍스트 갱신

## 4. 테스트 체크리스트
- [ ] 신규 체크 패턴이 정상 표시되는가
- [ ] 3색 체크에서 3개 색이 잘 반영되는가
- [ ] 다이아 체크가 마름모 느낌으로 출력되는가
- [ ] 색연필/파스텔 질감 체크가 정상 표시되는가
- [ ] 위치 X/Y 버튼(-5,-1,+1,+5)이 정상 동작하는가
- [ ] 숫자 입력, 슬라이더, 버튼 값이 서로 동기화되는가
- [ ] diagonal repeat가 정상 동작하는가
- [ ] 원형 프사 미리보기가 렌더링 결과를 잘 반영하는가
- [ ] PNG 저장과 심리스 타일 저장이 정상 동작하는가
