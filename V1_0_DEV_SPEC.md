# Cute Pattern Studio v1.0 실제 개발 명세서

## 1. 버전 목표

v1.0의 핵심 목표는 다음 4가지다.
1. 체크 패턴의 외곽선 색상을 따로 수정 가능하게 만들기
2. 체크 외곽선 두께를 조절 가능하게 만들기
3. 선 없는 체크 스타일도 쉽게 만들 수 있게 개선하기
4. 체크 패턴만 빠르게 고를 수 있는 체크 전용 필터 제공

---

## 2. 사용자 요구 반영 사항

### A. 체크 선 색상 수정
- 레이어 색상 슬롯을 5개로 확장
- 신규 슬롯 이름: `선 색`
- 체크 / 텍스처 체크 / 플래드 계열에서 선 색 사용

### B. 체크 선 두께 조절
- `선 두께` 슬라이더 범위를 `0~18px`로 변경
- `0`일 때는 라인이 완전히 사라져야 함
- `1 이상`일 때는 체크 라인이 보이도록 처리

### C. 선 없는 체크
- 기존 체크 패턴에서도 선 두께를 0으로 조절하면 선 제거 가능
- 추가로 바로 사용 가능한 프리셋 제공
  - `flat-checker`
  - `flat-tri-check`

### D. 체크 전용 필터
- 패턴 라이브러리 상단에 `체크 전용` 토글 추가
- 체크 관련 프리셋만 필터링해서 표시

---

## 3. 구현 명세

### index.html
- 버전 표기를 `v1.0`으로 변경
- 상단 설명 문구 갱신
- 필터 영역에 `체크 전용` 체크박스 추가
- 안내 박스의 개선 내용 갱신

### app.js
- LocalStorage 키를 `v10` 계열로 분리
- `normalizeColors()` 추가
  - 레이어 색상을 5개 구조로 정규화
- `checkPresetOnly()` 추가
  - 체크 계열 판별 함수
- `defaultLayer()`의 기본 색상 배열을 5개로 확장
- `mergeState()`에서 이전 버전 4색 레이어를 5색으로 보정
- `filteredPresets()`에 체크 전용 필터 반영
- `randomizeAll()`에 체크 전용 필터 반영
- `renderReferencePalette()` / 랜덤 팔레트 적용 시 5색 구조 반영
- `선 두께` 슬라이더 최소값을 0으로 확장

### patterns.js
- `checker()`
  - 외곽선 색상에 `colors[4]` 사용
  - 선 두께 0 이상일 때 stroke 렌더링
- `texturedChecker()`
  - grid 라인 색상에 `colors[4]` 사용
  - 선 두께 0일 때 라인 제거
- `plaid()`
  - fine/fabric 라인 색상에 `colors[4]` 사용
  - 선 두께 0일 때 라인 제거
- 신규 체크 프리셋 추가
  - `flat-checker`
  - `flat-tri-check`

---

## 4. 테스트 체크리스트

- [ ] 색상 편집에 `선 색` 슬롯이 보이는가
- [ ] 체크/플래드 계열에서 선 색 변경이 실제로 반영되는가
- [ ] 선 두께 0~18 조절이 정상 동작하는가
- [ ] 선 두께 0일 때 라인이 사라지는가
- [ ] 플랫 체크 / 플랫 3색 체크가 패턴 목록에 보이는가
- [ ] 체크 전용 토글이 정상 동작하는가
- [ ] 검색, 즐겨찾기 필터와 함께 사용해도 정상 동작하는가
- [ ] 랜덤 조합 기능이 오류 없이 동작하는가
- [ ] PNG 저장 / 심리스 타일 저장이 정상 동작하는가
