# Cute Pattern Studio v0.6 실제 개발 명세서

## 1. 버전 목표

v0.6의 핵심 목표는 **반복 요소 패턴의 정렬 품질을 실제 체감 수준으로 개선**하는 것이다.

이전 버전에서는 랜덤 토글을 꺼도 다음 문제가 남을 수 있었다.
- 요소 위치가 미세하게 흔들림
- 전체 레이어 회전값이 남아 있어서 살짝 기울어 보임
- 불규칙함 값이 남아 있어서 선/도형이 덜 정돈되어 보임
- 업로드 에셋 패턴은 동일한 정렬 UI 감각이 부족함

따라서 v0.6에서는 단순히 랜덤성만 끄는 수준이 아니라,
**실제로 더 반듯하고 깔끔해 보이는 정렬 상태**를 만들도록 렌더러와 UI를 함께 보정한다.

---

## 2. 기능 요구사항

### A. 반복 요소 정렬 제어
대상 레이어:
- 내장 패턴 타입 `motif`, `dots`, `raindrops`, `doodles`
- 업로드 에셋 레이어

레이어 편집 UI에 다음을 제공한다.
- `요소 크기 랜덤`
- `요소 각도 랜덤`
- `요소 위치 랜덤`
- `깔끔 정렬 적용`

### B. 깔끔 정렬 적용 동작 규칙
버튼 클릭 시 다음을 동시에 수행한다.
- `randomSize = false`
- `randomAngle = false`
- `randomPosition = false`
- `jitter = 0`
- `rotation = 0`

목표 결과:
- 같은 크기
- 같은 방향
- 같은 간격
- 기울어짐 없는 배열
- 흔들림 없는 반복 오브젝트 배치

### C. 레이어 순서 기능 유지
- 기본 레이어는 항상 가장 아래 유지
- 추가 레이어는 위/아래 이동 가능

---

## 3. 데이터 구조 명세

레이어 객체 필드:

```js
{
  randomSize: true,
  randomAngle: true,
  randomPosition: true
}
```

### 저장 정책
- `localStorage`에 포함
- 이전 버전 상태 복원 시 값이 없으면 `true`로 보정

---

## 4. UI/UX 명세

### 4-1. 반복 요소 정렬 섹션
표시 조건:
- 현재 레이어가 업로드 에셋이거나,
- 내장 패턴 타입이 `motif`, `dots`, `raindrops`, `doodles`

구성 요소:
- 섹션 제목: `반복 요소 정렬`
- 버튼: `깔끔 정렬 적용`
- 토글 3개
  - `요소 크기 랜덤`
  - `요소 각도 랜덤`
  - `요소 위치 랜덤`
- 설명 문구
  - 정렬 모드에서는 위치 흔들림, 불규칙함, 회전을 함께 줄여 더 반듯하게 보이도록 안내

### 4-2. 기대 UX
사용자는 다음 두 가지 흐름 중 하나를 쉽게 선택할 수 있어야 한다.
1. 랜덤하고 말랑한 감성의 패턴
2. 아주 깔끔하고 반듯한 정렬 패턴

---

## 5. 렌더링 엔진 수정 명세

### 5-1. 공통 보정 개념
`aligned` 조건:
- `randomPosition === false`
- 또는 `randomSize === false && randomAngle === false`

`aligned`일 때 공통 처리:
- 위치 jitter 제거
- 요소를 셀 경계가 아닌 **셀 중심(step / 2)** 기준으로 배치
- 패턴이 더 균일하게 보이도록 내부 랜덤성 축소

### 5-2. dots 렌더러
수정 내용:
- 정렬 모드에서 점 위치를 격자 중심에 배치
- 크기 랜덤 제거
- doodle dot의 외곽 흔들림도 최소화

### 5-3. motifScatter 렌더러
수정 내용:
- 정렬 모드에서 오브젝트를 격자 중심에 배치
- 크기 고정
- 회전 고정
- strawberry 내부 씨앗 흔들림 제거
- moon/star 혼합 계열은 정렬 모드에서 더 안정적인 한 방향 표현 우선

### 5-4. raindrops 렌더러
수정 내용:
- 정렬 모드에서 중심 정렬 배치
- 위치 흔들림 제거

### 5-5. doodles 렌더러
수정 내용:
- 정렬 모드에서 중심 정렬 배치
- 회전 고정
- 크기 고정
- 랜덤한 기호 섞임을 줄여 배열감이 무너지지 않도록 보정

### 5-6. 업로드 에셋 렌더러
수정 내용:
- 업로드 에셋 레이어에도 정렬 토글 적용
- 정렬 모드에서 위치 흔들림 제거
- `motif` 방식일 경우 격자 중심 정렬 적용
- `tile` 방식은 타일 반복 특성을 유지하면서 흔들림만 제거

---

## 6. 앱 로직 수정 명세

### app.js 변경점
1. `layerSupportsScatterControls()`
- 업로드 에셋 레이어도 `true` 반환

2. `renderUploadedLayer()`
- `cleanAlign` 개념 추가
- 정렬 모드에서 jitter 제거
- motif 반복에서 중심 배치 적용

3. `renderLayerEditor()`
- 정렬 섹션 안내 문구 업데이트
- `깔끔 정렬 적용` 버튼 클릭 시 jitter/rotation까지 0으로 설정

4. 기존 레이어 추가/삭제/이동 기능 유지

### patterns.js 변경점
- `dots()`
- `motifScatter()`
- `raindrops()`
- `doodles()`
각 함수에 중심 정렬 + 랜덤성 제거 보정 반영

---

## 7. 스스로 분석해 추가 반영한 개선점

1. **업로드 에셋 정렬 UX 통일**
   - 사용자는 내장 패턴뿐 아니라 직접 넣은 PNG/SVG도 같은 방식으로 정렬하고 싶어할 가능성이 큼
   - 따라서 업로드 레이어에도 동일한 정렬 제어 UI를 제공

2. **정렬 버튼의 기대 결과 강화**
   - 단순히 랜덤만 끄는 것이 아니라
   - 실제로 “반듯하게 보이는 결과”가 중요함
   - 그래서 jitter와 rotation도 함께 0으로 맞춤

3. **중심 배치 보정**
   - 동일 간격 패턴은 셀 꼭짓점보다 셀 중심에 배치될 때 더 안정감 있게 보임
   - 정렬 모드에서는 이를 우선 적용

---

## 8. 테스트 체크리스트

### 정렬 기능 테스트
- [ ] 하트 패턴에서 깔끔 정렬 적용 시 기울기 없이 정렬되는가
- [ ] 별 패턴에서 같은 크기 / 같은 방향으로 정렬되는가
- [ ] 도트 패턴에서 점 크기가 일정한가
- [ ] 손그림 도트에서 외곽 흔들림이 줄어드는가
- [ ] 물방울 패턴에서 위치 흔들림이 제거되는가
- [ ] 낙서 패턴에서 회전이 사라지고 배열감이 안정되는가
- [ ] 업로드 PNG 오브젝트 패턴에서 위치 흔들림이 제거되는가
- [ ] 깔끔 정렬 적용 버튼이 jitter와 rotation까지 0으로 만드는가

### 회귀 테스트
- [ ] 배경색 포함 PNG 저장이 정상인가
- [ ] 투명 배경 저장이 정상인가
- [ ] 심리스 타일 저장이 정상인가
- [ ] SVG 단색 변환이 정상인가
- [ ] 프리셋 저장/불러오기가 정상인가
- [ ] 팔레트 추출이 정상인가
- [ ] 레이어 순서 이동이 정상인가

---

## 9. 다음 버전(v0.7) 후보
- 업로드 에셋용 brick / half-drop / diagonal repeat
- 레이어 잠금 / 숨김 / 이름 직접 수정
- 원형 프사 미리보기
- 색상 자동 파스텔화
- JSON 프리셋 내보내기 / 가져오기
