# Cute Pattern Studio v0.4 업데이트 정리 + 다음 단계 계획서

## 1. 이번 요청에서 중요했던 문제

### A. 레이어 구조가 너무 고정적이었음
기존 구조는 3개 레이어가 처음부터 존재하는 방식이라,
사용 입장에서 **기본 한 장으로 가볍게 시작하고 필요할 때만 추가**하는 흐름이 아니었다.

### B. 배경색 미리보기와 저장 결과가 일치하지 않았음
사용자는 배경색과 패턴의 조합을 보면서 작업하고 싶었지만,
실제 저장된 PNG는 배경이 빠져서 결과가 달라지는 문제가 있었다.

### C. 귀여운 패턴 종류를 조금 더 확장하고 싶었음
현재도 충분히 좋지만,
프사 배경이나 귀여운 디자인용으로 쓰기 위해서는 더 다양한 선택지가 필요했다.

---

## 2. 이번 v0.4에서 실제 반영한 내용

### 2-1. 레이어 구조 개선
- 기본 레이어 1개만 시작
- 추가 레이어는 필요할 때만 생성
- 기본 레이어는 항상 활성
- 추가 레이어는 켜기/끄기/삭제 가능
- 현재 레이어 복제 추가 가능
- 탭/요약 UI에서 현재 총 레이어 수 확인 가능

### 2-2. 배경색 저장 버그 수정
핵심 수정 포인트:
- 패턴 레이어 렌더링이 배경을 덮어쓰거나 지우지 않도록 수정
- 미리보기에서 보는 배경색 조합이 저장 결과와 동일하게 나오도록 수정
- 투명 배경을 끄면 **배경색 + 패턴 합성 PNG** 저장
- 투명 배경을 켜면 **투명 패턴 PNG** 저장

### 2-3. 패턴 종류 추가
추가한 예시:
- 키라 스파클
- 잔리본
- 퍼프 하트
- 캔디 스타
- 데이지 도트

### 2-4. 기존 기능 유지
- 업로드 에셋 반복 패턴화
- SVG 색상 변경
- 심리스 타일 저장
- 즐겨찾기 / 내 프리셋
- 참고 이미지 팔레트 추출

---

## 3. v0.5 후보 기능 목록

### 1순위
1. **업로드 에셋 배치 방식 확대**
   - brick
   - half-drop
   - diagonal repeat
   - random scatter
2. **레이어 순서 변경**
   - 위로 / 아래로
   - 맨 위 / 맨 아래
3. **프사 미리보기 기능**
   - 원형 크롭 미리보기
   - 테두리/장식 프레임 겹쳐보기
4. **체크/도트 전용 세부 옵션 강화**
   - 가로/세로 두께 분리
   - 보조선 추가
   - 칸 내부 포인트 밀도 조절

### 2순위
1. **색상 보정 도우미**
   - 자동 파스텔화
   - 명도/채도 간단 조절
   - 최근 사용 색상
2. **프리셋 입출력**
   - JSON 내보내기
   - JSON 가져오기
3. **업로드 이미지 꾸미기**
   - 흰 테두리
   - 그림자
   - 외곽선

### 3순위
1. **심리스 타일 미리보기 전용 화면**
2. **조합 추천 세트**
   - 프사 배경용
   - 키치/Y2K용
   - 파스텔용
3. **움짤 배경용 애니메이션 패턴 시퀀스 기획**

---

## 4. 개발 메모
- 정적 웹앱 구조 유지 (GitHub + Cloudflare Pages 배포 용이)
- localStorage 용량 한계가 보이면 IndexedDB 검토
- 업로드 에셋 배치 다양화 시 렌더 성능 최적화 필요
