# Cute Pattern Studio v0.5 실제 개발 명세서

## 1. 프로젝트 개요

### 프로젝트명
Cute Pattern Studio v0.5

### 목표
귀여운 패턴 배경을 쉽고 빠르게 만드는 정적 웹앱을 유지하면서,
특히 **반복 요소 패턴에서 개별 요소의 랜덤성 제어**를 강화한다.

사용자는 아래를 할 수 있어야 한다.
- 배경색이 포함된 2000×2000 PNG 배경 생성
- 기본 레이어 1개에서 시작하고 필요 시 레이어 추가
- 하트/별/도트/리본 등 반복 요소 패턴의
  - 크기 랜덤 여부
  - 각도 랜덤 여부
  를 직접 제어
- 업로드 SVG/PNG/JPG를 반복 패턴으로 활용

---

## 2. 이번 버전의 핵심 요구사항

### 기능 요구사항 A — 반복 요소 정렬 옵션
대상 패턴:
- 하트
- 별
- 반짝이
- 리본
- 꽃
- 도트 계열
- 물방울 등 반복 요소 기반 패턴

레이어 편집 UI에 다음 옵션을 추가한다.
- `요소 크기 랜덤` (boolean)
- `요소 각도 랜덤` (boolean)

동작 규칙:
- 둘 다 ON → 기존처럼 자연스러운 랜덤 배치 유지
- `요소 크기 랜덤` OFF → 모든 반복 요소가 동일한 크기 사용
- `요소 각도 랜덤` OFF → 모든 반복 요소가 동일한 방향 사용
- 둘 다 OFF → 같은 크기 + 같은 방향으로 정렬된 규칙적 패턴 생성

### 기능 요구사항 B — 이전 버전 핵심 기능 유지
- 배경색 포함 PNG 저장
- 투명 배경 저장
- 레이어 추가/삭제
- 업로드 에셋 반복 패턴화
- SVG 색상 단색화
- 프리셋 저장
- 즐겨찾기
- 팔레트 자동 추출
- 심리스 타일 PNG 저장

---

## 3. 데이터 구조 명세

레이어 객체에 다음 프로퍼티를 추가한다.

```js
{
  randomSize: true,
  randomAngle: true
}
```

### 의미
- `randomSize`
  - `true`: 요소 크기에 랜덤 변형 적용
  - `false`: 고정 크기 사용
- `randomAngle`
  - `true`: 요소 각도에 랜덤 변형 적용
  - `false`: 고정 각도 사용

### 저장 정책
- `localStorage`의 상태 저장에 포함
- 이전 버전 상태 로드 시 값이 없으면 기본값 `true`로 보정

---

## 4. UI/UX 명세

### 레이어 편집 섹션
조건:
- 현재 레이어가 내장 패턴이며
- 패턴 타입이 `motif`, `dots`, `raindrops` 중 하나일 때만 표시

표시 UI:
- 섹션명: `반복 요소 정렬`
- 스위치 1: `요소 크기 랜덤`
- 스위치 2: `요소 각도 랜덤`
- 안내 문구:
  - `둘 다 끄면 모든 요소가 같은 크기와 같은 방향으로 정렬돼.`

### UX 목적
- 랜덤한 귀여움과 정렬된 깔끔함 중 사용자가 선택 가능
- 규칙적인 패턴 배경 제작을 더 쉽게 함

---

## 5. 렌더링 엔진 명세

### patterns.js 수정 포인트

#### dots 렌더러
- 기존: irregular 옵션에서 점 크기 랜덤 사용
- 변경:
  - `randomSize === false`이면 랜덤 반지름 변형 제거
  - 동일한 점 크기로 출력

#### motifScatter 렌더러
- 기존:
  - 크기: `.28 ~ .48` 범위 랜덤
  - 각도: `detail` 기반 랜덤 회전
- 변경:
  - `randomSize === false` → 고정 비율 크기 사용
  - `randomAngle === false` → 회전값 0 사용

### 배경 렌더링 정책
- 전체 캔버스는 앱의 배경 설정으로 먼저 렌더
- 레이어 패턴은 **배경을 다시 칠하지 않고** 오버레이만 수행
- 저장 시 미리보기와 같은 합성 구조를 유지

---

## 6. 앱 로직 명세

### app.js 수정 포인트

1. `defaultLayer()`
- `randomSize: true`
- `randomAngle: true`
추가

2. `mergeState()`
- 이전 저장 데이터 복원 시
  - `randomSize` 없으면 `true`
  - `randomAngle` 없으면 `true`

3. `layerToPatternState()`
- 렌더러에 `randomSize`, `randomAngle` 전달

4. `renderLayerEditor()`
- 해당 패턴 타입에서만 정렬 옵션 UI 렌더
- 토글 변경 시 즉시 미리보기 반영

5. `randomizeAll()`
- 랜덤 생성 시 두 옵션은 기본적으로 `true`로 초기화

---

## 7. 파일 구성 명세

### 주요 파일
- `index.html`
  - 버전 표기 및 안내 문구 업데이트
- `app.js`
  - 상태/레이어/UI 제어 로직
- `patterns.js`
  - 반복 요소 랜덤성 제어 반영
- `README.md`
  - 사용 설명 업데이트
- `V0_5_DEV_SPEC.md`
  - 본 명세서

---

## 8. 테스트 체크리스트

### 기능 테스트
- [ ] 기본 레이어 1개로 시작하는가
- [ ] 레이어 추가/삭제가 정상 동작하는가
- [ ] 배경색 포함 PNG 저장이 정상 동작하는가
- [ ] 투명 배경 저장이 정상 동작하는가
- [ ] 하트/별/리본 패턴에서 `요소 크기 랜덤` OFF 시 크기가 동일한가
- [ ] 하트/별/리본 패턴에서 `요소 각도 랜덤` OFF 시 방향이 동일한가
- [ ] 두 옵션 모두 OFF 시 정렬된 패턴이 생성되는가
- [ ] 도트 패턴에서 `요소 크기 랜덤` OFF 시 동일한 점 크기로 렌더되는가
- [ ] 설정 저장 후 새로고침해도 옵션 상태가 유지되는가

### 회귀 테스트
- [ ] 업로드 PNG/SVG 반복 패턴화가 여전히 정상인가
- [ ] SVG 단색 변환이 정상인가
- [ ] 프리셋 저장/불러오기가 정상인가
- [ ] 팔레트 추출이 정상인가
- [ ] 심리스 타일 저장이 정상인가

---

## 9. 다음 버전(v0.6) 추천 개발 항목
- 업로드 에셋용 brick / half-drop / diagonal repeat
- 레이어 순서 이동
- 프사 원형 크롭 미리보기
- 색상 자동 파스텔화
- JSON 프리셋 입출력
