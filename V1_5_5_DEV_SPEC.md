# Cute Pattern Studio v1.5.6 개발 명세서

## 변경 목적
소프트 썬버스트처럼 패턴 내부에서 두 색이 번갈아 사용되는 패턴을 한 가지 대표색만으로 제한하지 않고, 두 색을 각각 직접 편집할 수 있게 한다.

## 대상 패턴
- `soft-sunburst-bg` 소프트 썬버스트
- `sunburst-bg` 썬버스트 배경

## UI
레이어 편집 > 레이어 색상 아래에 `햇살 2색 직접 조절` 전용 영역을 표시한다.
- 햇살 색상 1: 기본 햇살
- 햇살 색상 2: 교차 햇살/깊은 톤
- 각 색상은 컬러 피커와 HEX 입력을 모두 제공

## 렌더링
기존 렌더러의 `s.colors[1]`, `s.colors[2]`를 그대로 사용하므로 UI에서 변경한 두 색상이 즉시 패턴에 반영된다.
