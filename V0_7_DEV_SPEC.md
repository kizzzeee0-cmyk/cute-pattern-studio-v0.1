# Cute Pattern Studio v0.7 실제 개발 명세서

## 1. 버전 목표

v0.7의 핵심 목표는 **패턴 반복 시작 위치를 사용자가 직접 미세 조정**할 수 있게 만드는 것이다.

이전 버전까지는 크기, 간격, 회전, 정렬 옵션은 있었지만,
패턴 자체의 시작 위치를 옮길 수는 없어서 다음 문제가 있었다.
- 체크 칸이 가장자리에서 애매하게 잘림
- 하트/별/도트가 화면 끝에서 보기 애매하게 배치됨
- 업로드한 오브젝트 패턴도 반복 위치를 조금만 옮기고 싶은 상황이 있음

따라서 이번 버전에서는 **레이어별 X/Y 오프셋 기능**을 추가한다.

---

## 2. 기능 요구사항

### A. 레이어별 패턴 위치 조절
모든 레이어 객체에 다음 값을 추가한다.
- `offsetX`
- `offsetY`

의미:
- 패턴 전체를 좌우로 이동
- 패턴 전체를 상하로 이동

### B. 적용 대상
- 내장 패턴 전체
- 오브젝트 반복 패턴
- 업로드한 PNG / SVG 에셋 반복 패턴

### C. UI 요구사항
레이어 편집 영역에 다음 슬라이더를 추가한다.
- `위치 X` (범위: -200 ~ 200 px)
- `위치 Y` (범위: -200 ~ 200 px)

안내 문구:
- `패턴이 애매하게 잘려 보일 때는 위치 X/Y를 살짝 움직여 반복 시작 위치를 조정할 수 있어.`

---

## 3. 데이터 구조 명세

레이어 객체 예시:

```js
{
  offsetX: 0,
  offsetY: 0
}
```

### 저장 정책
- `localStorage`에 포함
- 이전 버전 상태 로드 시 값이 없으면 `0`으로 초기화

---

## 4. 앱 로직 수정 명세

### app.js 변경점
1. `defaultLayer()`
- `offsetX: 0`
- `offsetY: 0`
추가

2. `mergeState()`
- 이전 저장 데이터에 값이 없으면 `0`으로 보정

3. `layerToPatternState()`
- 렌더러로 `offsetX`, `offsetY` 전달

4. `renderLayerEditor()`
- `위치 X`, `위치 Y` 슬라이더 추가
- 이동 시 즉시 미리보기 반영

5. `randomizeAll()`
- 랜덤 생성 시 위치 값은 `0`으로 초기화

6. `renderUploadedLayer()`
- 업로드 에셋 반복 렌더링에 `offsetX`, `offsetY` 반영
- motif 배치 / tile 배치 모두 위치 이동 가능

---

## 5. 렌더링 엔진 수정 명세

### patterns.js 변경점
`render()` 진입 시,
현재 레이어의 위치 보정값을 캔버스 변환에 적용한다.

동작 방식:
- `ctx.translate((s.offsetX||0)*scale, (s.offsetY||0)*scale)`
- 이후 패턴 렌더링 수행

효과:
- 내장 패턴 전체가 동일하게 위치 이동
- 기존 패턴 알고리즘을 크게 바꾸지 않으면서 시작 위치 조절 가능

### 업로드 에셋 렌더러
- 반복 기준 좌표에 `offsetX`, `offsetY` 추가
- 패턴 정렬 모드와 함께 사용할 수 있음

---

## 6. 기대 UX

사용자는 이제 다음과 같이 작업할 수 있어야 한다.
- 체크 패턴의 칸이 가장자리에서 예쁘게 보이도록 조금 이동
- 하트/별 패턴의 배치 시작점을 미세 조절
- 업로드한 스티커/오브젝트 패턴의 반복 위치를 맞춤 조정
- 기존 정렬 기능과 함께 사용해서 더 깔끔한 배경 제작

---

## 7. 테스트 체크리스트

### 위치 조절 테스트
- [ ] 위치 X 슬라이더를 움직이면 패턴이 좌우로 이동하는가
- [ ] 위치 Y 슬라이더를 움직이면 패턴이 상하로 이동하는가
- [ ] 내장 체크 패턴에서 시작 위치가 바뀌는가
- [ ] 도트 / 하트 / 별 패턴에도 정상 적용되는가
- [ ] 업로드한 PNG / SVG 패턴에도 정상 적용되는가
- [ ] 저장된 PNG 결과가 미리보기와 동일한가
- [ ] 심리스 타일 저장에서도 오프셋이 반영되는가

### 회귀 테스트
- [ ] 배경색 포함 PNG 저장이 정상인가
- [ ] 투명 배경 저장이 정상인가
- [ ] 깔끔 정렬 적용이 정상인가
- [ ] 레이어 추가/삭제/순서 이동이 정상인가
- [ ] 팔레트 추출 / 프리셋 저장이 정상인가

---

## 8. 다음 버전(v0.8) 후보
- 위치 X/Y 입력칸 직접 숫자 입력 지원
- 오프셋 초기화 버튼
- 업로드 에셋용 brick / half-drop / diagonal repeat
- 원형 프사 미리보기
- JSON 프리셋 내보내기 / 가져오기
