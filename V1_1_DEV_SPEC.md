# Cute Pattern Studio v1.1 실제 개발 명세서

## 1. 목표
v1.1의 목표는 체크/패턴 작업에서 색 조합 선택을 더 쉽게 만드는 것이다.

핵심 목표:
1. 현재 레이어 기준 `선 색 자동 추천`
2. 함께 쓰기 좋은 `컬러 조합 프리셋`
3. 한 가지 색상만으로 만드는 `자동 추천 팔레트`
4. 추천 결과를 현재 레이어 또는 배경+레이어에 즉시 적용

---

## 2. 사용자 요구 반영 사항

### A. 선 색 자동 추천
- 레이어 편집 > 색상 영역에 `선 색 자동 추천` 버튼 추가
- 패턴 A / 패턴 B / 포인트색을 혼합 분석해서 선 색 자동 계산
- 너무 튀지 않으면서 체크 라인이 잘 보이는 색으로 추천

### B. 예쁜 조합 프리셋
- 미리 준비된 컬러 조합 카드 제공
- 각 카드에 5색(배경A / 패턴A / 패턴B / 포인트 / 선색) 포함
- 버튼 제공
  - `레이어 적용`
  - `배경+레이어`

### C. 기준 색상 자동 추천
- 기준 색상 컬러피커 추가
- 기준 색상 변경 시 자동 추천 팔레트 갱신
- 추천 유형
  - 소프트 단색
  - 비슷한 색 조합
  - 포인트 대비
  - 캔디 믹스

---

## 3. 구현 명세

### index.html
- 버전 표기 `v1.1`로 변경
- 컬러 추천 섹션 추가
  - `baseColorInput`
  - `refreshColorSuggestionBtn`
  - `curatedPaletteList`
  - `autoPaletteList`
- v1.1 개선 내용 안내 박스 업데이트

### app.js
- LocalStorage 키를 `cps-v11-*`로 분리
- 신규 데이터
  - `curatedHarmonyPresets`
- 신규 함수
  - `hexToRgb()`
  - `rgbToHexObj()`
  - `rgbToHsl()`
  - `hslToRgb()`
  - `hslToHex()`
  - `mixHex()`
  - `suggestLineColor()`
  - `generateAutoPalettes()`
  - `applyPaletteToCurrentLayer()`
  - `renderPaletteCard()`
  - `renderColorRecommendationPanels()`
- 레이어 편집 UI에 `선 색 자동 추천` 버튼 추가
- 팔레트 랜덤 / 참고이미지 팔레트 적용 시 선 색 자동 보정 적용

### styles.css
- 기준 색상 선택용 `#baseColorInput` 스타일 추가

---

## 4. 동작 규칙

### 선 색 자동 추천 로직
- 패턴 A / 패턴 B 색을 중심으로 후보색 생성
- 후보색을 HSL 기준으로 살짝 더 어둡고 차분하게 조정
- 체크 라인에 어울리는 선 색으로 변환

### 자동 팔레트 생성 로직
입력한 기준 색상에서 hue/saturation/lightness를 변형해 다음 팔레트를 생성한다.
- 단색 확장
- 유사색 확장
- 포인트 대비 확장
- 캔디 믹스 확장

---

## 5. 테스트 체크리스트
- [ ] `선 색 자동 추천` 버튼이 보이는가
- [ ] 버튼 클릭 시 선 색이 실제로 바뀌는가
- [ ] 컬러 추천 섹션이 보이는가
- [ ] 기준 색상을 바꾸면 자동 추천 팔레트가 갱신되는가
- [ ] 추천 카드의 `레이어 적용`이 정상 동작하는가
- [ ] 추천 카드의 `배경+레이어`가 정상 동작하는가
- [ ] 저장 / 랜덤 / 참고이미지 팔레트 적용 후에도 오류가 없는가
