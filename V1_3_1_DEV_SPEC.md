# Cute Pattern Studio v1.3.1 개발 명세서

## 목표
v1.3.1의 목표는 두 가지다.
1. 왼쪽 패턴 라이브러리 UI가 좁아 카드가 살짝 잘려 보이는 문제를 완화한다.
2. 기존 3색 체크가 단순히 색면이 나란히 이어지는 느낌이 아니라, 첨부 예시처럼 **같은 색 밴드가 교차하며 3톤이 자연스럽게 생기는 체크**를 제공한다.

## 핵심 기능
### 1) 패턴 라이브러리 UI 개선
- workspace 첫 번째 컬럼 폭 확대
- 패턴 카드 패딩/텍스트 크기 조정
- 반응형 전환 지점 조정

### 2) overlap-check 렌더러 추가
새 렌더 타입 `overlap-check`를 추가한다.
- 배경색 위에 같은 계열의 세로 밴드를 1칸 걸러 배치
- 같은 방식으로 가로 밴드를 1칸 걸러 배치
- 밴드가 겹치는 칸은 더 진해지고, 겹치지 않는 칸은 연한 톤으로 남음
- 결과적으로 첨부 예시 같은 3톤 체크 형성

### 3) 체크 프리셋 개편
기존 3색 계열 일부를 겹침형으로 변경
- `tri-color-check` → `3톤 겹침 체크`
- `flat-tri-check` → `플랫 3톤 체크`
- `no-gap-tri-check` → `노갭 3톤 체크`

추가 프리셋
- `soft-overlap-check`
- `airy-overlap-check`

## 기대 결과
- 사용자가 대표색 하나만 골라도 첨부 예시와 가까운 체크 패턴을 빠르게 제작 가능
- 패턴 라이브러리 카드 잘림/답답함이 줄어듦
