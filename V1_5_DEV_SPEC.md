# Cute Pattern Studio v1.5 개발 명세서

## 목표
v1.5의 목표는 사용자가 첨부한 예시처럼,
**원형 프사 안쪽을 햇살/광택 느낌으로 채우는 비-타일형 장식 패턴**을 기본 프리셋으로 제공하는 것이다.

## 추가 프리셋
### 1) sunburst-circle
- 카테고리: `원형 프사`
- 특징:
  - 방사형 햇살 조각
  - 중앙 화이트 글로우
  - 오렌지 외곽 링
  - 작은 반짝이 포인트

### 2) glossy-sun-circle
- 카테고리: `원형 프사`
- 특징:
  - 부드러운 원형 그라데이션 면
  - 가장자리 광택 스트릭
  - 작은 주황 포인트 도트
  - 반짝이 포인트

## 구현 내용
1. `patterns.js`에 색상 보간용 헬퍼 추가
   - `hexToRgb()`
   - `rgbToHex()`
   - `mixColor()`
2. 신규 렌더 함수 추가
   - `sunburstCircle()`
   - `glossyCircle()`
3. 렌더 스위치에 신규 타입 연결
   - `sunburst-circle`
   - `glossy-circle`
4. 프리셋/기본값 추가
   - 신규 카테고리 `원형 프사` 노출

## 조정 가능한 요소
- 크기: 원의 전체 반지름
- 회전: 햇살 / 광택의 방향
- 디테일: 햇살 분할 밀도
- 선 두께: 원형 외곽선 두께
- 색상: 노랑, 주황, 반짝이, 테두리 톤 변경 가능

## 기대 효과
- 움짤 프사/닉네임 원형 배경을 앱 안에서 바로 제작 가능
- 기존 체크/도트 패턴과 다른 `중앙 집중형` 스타일 제공
