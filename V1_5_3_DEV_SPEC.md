# Cute Pattern Studio v1.5.3 개발 명세서

## 기능
배경에 원하는 색상의 컬러 비네트를 추가한다. 원형 프레임이 아니라 현재 네모 캔버스 전체에 적용된다.

## 상태 데이터
`bg.vignette = { enabled, color, range, strength, softness }`

- `enabled`: 사용 여부
- `color`: HEX 색상
- `range`: 비네트 시작 범위(0~100). 낮을수록 중앙까지 효과가 들어옴.
- `strength`: 가장자리 색의 불투명도(0~100)
- `softness`: 시작 범위부터 가장자리까지의 전환 폭(0~100)

## 렌더링
배경을 먼저 그린 뒤 radial gradient를 한 번 더 합성한다. 중앙은 투명하고 가장자리로 갈수록 지정 색상이 강해진다. 투명 배경 모드에서는 비네트를 적용하지 않는다.

## 저장
메인 PNG와 심리스 타일 PNG 모두 동일한 배경 렌더링 경로를 사용하므로 비네트 설정이 결과물에도 반영된다.
