# Cute Pattern Studio v1.5.1 개발 명세서

## 1. 수정 목적
v1.5에서 두 문제가 발생했다.
1. 좌측 패턴 라이브러리의 전체 목록이 정상 노출되지 않음
2. 기존 패턴들이 미리보기 캔버스에 렌더링되지 않는 문제가 발생

또한 신규 디자인을 `원형 프사 프레임`으로 잘못 해석한 부분을 수정한다.
사용자가 원하는 것은 **네모 캔버스는 그대로 유지하되, 첨부 이미지 안쪽의 햇살/광택 표현을 참고한 배경 패턴**이다.

## 2. 복구 전략
- v1.5에서 수정된 DOM/렌더 구조를 이어서 고치는 대신, 안정적으로 동작하던 v1.4를 베이스로 재구성
- 기존 PatternEngine 프리셋/렌더 디스패처를 유지
- 신규 타입만 최소 추가

## 3. 신규 패턴 타입
### sunburst-bg
- 캔버스 전체에 radial gradient를 먼저 채움
- 중심에서 화면 모서리보다 먼 거리까지 wedge를 그려 네모 전체를 채움
- 원형 clip / 외곽 원 테두리를 사용하지 않음
- 중앙 white glow와 sparkle 포인트 추가

### soft-sunburst-bg
- sunburst-bg보다 대비가 낮은 파스텔형
- 방사선 투명도를 낮춰 부드럽게 표현

### glossy-sun-bg
- 캔버스 전체 radial gradient
- 중앙 밝은 광원
- 가장자리 쪽에 광택 곡선(highlight stroke)
- 작은 sparkle / glossy dot 포인트
- 원형 프레임 없음

### sparkle-glow-bg
- 중앙 glow 중심의 단순한 버전
- 가벼운 반짝이만 배치

## 4. 신규 카테고리
`빛·광택`
- 썬버스트 배경
- 소프트 썬버스트
- 글로시 선샤인
- 스파클 글로우

## 5. 회귀 테스트
- [ ] 초기 진입 시 `전체` 패턴 목록이 표시되는가
- [ ] 기존 `파스텔 체크` 썸네일이 표시되는가
- [ ] 기존 `미니 하트`, `구름`, `도트` 썸네일이 표시되는가
- [ ] 각 기존 패턴 클릭 시 메인 미리보기에 나타나는가
- [ ] 신규 빛·광택 패턴이 네모 전체를 채우는가
- [ ] 신규 패턴에 원형 clip / 외곽 원 테두리가 없는가
- [ ] 카테고리 전환 후 다시 `전체`로 돌아올 수 있는가
