# Cute Pattern Studio v1.5.8 개발 명세서

## 변경 목표
소프트 썬버스트의 두 색상을 완전히 독립적으로 지정할 수 있도록 수정한다.

## 문제
v1.5.5의 소프트 썬버스트 렌더러가 색상 사이를 `mixColor()`로 보정하여, 두 번째 색상을 흰색으로 지정해도 첫 번째 색상의 영향을 받은 것처럼 보일 수 있었다.

## 수정
- `softSunburstBackground()`를 직접 2색 렌더 방식으로 변경
- `color1 = s.colors[1]`, `color2 = s.colors[2]`를 그대로 사용
- 기본 바탕과 교차 방사선에 두 색상을 직접 적용
- 색상 1 변경 시 색상 2를 자동 보정하지 않음
- 중앙 글로우는 별도의 미세한 흰색 오버레이로만 처리

## 예시
- 색상 1: `#FFD95A`
- 색상 2: `#FFFFFF`
→ 노랑 + 하얀색 조합 그대로 렌더링
